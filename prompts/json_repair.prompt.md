Repair invalid JSON using the Schema error message. Preserve valid content and add no creative
ideas. Fix only missing fields, types, enum values, malformed JSON, and obvious ID mismatches.
Return valid JSON only.
