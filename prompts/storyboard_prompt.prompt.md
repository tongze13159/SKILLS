Generate first-frame storyboard prompts for every new shot. Show the action start state, never the
completed action. Include product invariants, avoid-deformation rules, scene, camera, lighting,
hand position, no visible face, no watermark, and originality rules. Return valid JSON only.
